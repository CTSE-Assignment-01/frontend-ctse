# Stage 1: Build
FROM node:20-alpine AS builder
WORKDIR /app
COPY . .
RUN npm install && npm run build

# Stage 2: Serve with Vite's preview server
FROM node:20-alpine
WORKDIR /app
COPY --from=builder /app /app
RUN npm install -g serve
EXPOSE 4173
CMD ["npm", "run", "preview"]
